// export default function ({ from }) {
//   const scrollPos = from?.meta[0].scrollPos;
//   if (scrollPos && Object.keys(scrollPos).length) {
//     window.scrollTo(scrollPos.x, scrollPos.y);
//   }
// }
